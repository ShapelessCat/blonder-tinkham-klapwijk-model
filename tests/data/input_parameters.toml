[shared_parameters]
max_voltage = 30
n_points = 151
d_temperature = 10
temperature = 5
angle = 45         # degree - numpy APIs needs radian

[[atomic_orbital_specific_parameters]]
proportion = 0.5
broadening_parameter = 0.01  # Γ (meV)
barrier_strength = 20        # Z - dimensionless
gap_config = { gap_plus = 4, gap_minus = 5 } # Δ (meV)
atomic_orbital = "s"
#formula_type = { epsilon = 0.01, symmetry = 2 }

[[atomic_orbital_specific_parameters]]
proportion = 0.5
broadening_parameter = 0.01  # Γ (meV)
barrier_strength = 20        # Z - dimensionless
gap_config = { gap = 3 }     # Δ (meV)
atomic_orbital = "s"

#[[atomic_orbital_specific_parameters]]
#proportion = 0.4
#broadening_parameter = 0.01                  # Γ (meV)
#barrier_strength = 20                        # Z - dimensionless
#gap_config = { gap_plus = 8, gap_minus = 8 } # Δ (meV)
#atomic_orbital = "d"
